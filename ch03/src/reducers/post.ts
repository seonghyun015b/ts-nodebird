const initialState = {
  mainPosts: [],
};

type Action = {
  type: null;
};

const reducer = (state = initialState, action: Action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
